# if [[ "$HOME" ]] && [[ -f ${MAS_ETC_BASH:=/etc/mastar/shell/bash}/.topparamfuncs ]] ; then
# . ${MAS_ETC_BASH:=/etc/mastar/shell/bash}/.topparamfuncs
  mas_sourcing_start "(binding) genterm"
# mas_gterminal -cmasGtermws -rgtermws  -p$MAS_DESKTOP_NAME $@
  mas_sourcing_end "(binding) genterm"
# fi
  return 0

# vi: ft=sh
